# بخش تجسمات آموزشی - راهنما

## 📊 تجسمات جدید اضافه شده

### 1. **BacterialVisualizer** (`src/ui/managers/bacterial-visualizer.js`)
ماژول تجسم حرکت باکتری‌ها با ویژگی‌های:

**ویژگی‌های کلیدی:**
- 🦠 نمایش باکتری‌های متعدد به رنگ‌های مختلف
- 🎯 نشانه غذایی در مرکز صفحه
- 📍 رسم مسیر حرکت هر باکتری
- 📊 نمایش آمار حرکت (مرحله، بهترین فاصله، تعداد باکتری)
- 🎨 پس‌زمینه شطرنجی با تم پویا (روشن/تاریک)

**رفتار الگوریتم:**
- باکتری‌ها به سمت غذا حرکت می‌کنند (Run)
- اگر فاصله کم شود، ادامه می‌دهند
- اگر فاصله زیاد شود، جهت را تغییر می‌دهند (Tumble)

**کنترل‌ها:**
```javascript
visualizer.start()      // شروع انیمیشن
visualizer.stop()       // توقف انیمیشن
visualizer.reset()      // تازه‌سازی
visualizer.update()     // بروزرسانی یک مرحله
visualizer.draw()       // رسم صفحه
```

---

### 2. **BehaviorVisualizer** (`src/ui/managers/behavior-visualizer.js`)
ماژول نمایش مراحل رفتار باکتریایی گام به گام.

**چهار مرحله الگوریتم:**

#### 🔵 مرحله 1: شیمی‌جویی (Chemotaxis)
- باکتری سبز: حالت Run (حرکت مستقیم)
- باکتری آبی: حالت Tumble (چرخش)
- نمایش شیوه نزدیک‌شدن به منبع غذایی
- نمایش gradent شیمیایی

#### 🟠 مرحله 2: سواریدگی (Swarming)
- باکتری قرمز در مرکز: رهبر موفق
- باکتری‌های نارنجی: دنبال‌کنندگان
- خطوط اتصال به رهبر
- نمایش جاذبیت باکتری‌های موفق

#### 🟣 مرحله 3: تکثیر (Reproduction)
- سلول والد (بنفش): تقسیم می‌شود
- سلول فرزند (آبی): از والد متولد می‌شود
- نمایش روند تقسیم سلولی
- انتقال ژن‌های بهتری

#### 🔴 مرحله 4: حذف و پراکندگی (Elimination & Dispersal)
- باکتری‌های ضعیف (قرمز): حذف می‌شوند
- باکتری‌های جدید (آبی): وارد محیط می‌شوند
- حفظ تنوع جمعیت
- گریز از تله‌های محلی

---

### 3. **EducationManager** (بخش نمایش‌دهندگی)
ماژول مدیریت تجسمات در صفحه درس.

**وظایف:**
- نمایش بوم‌نقاشی Canvas
- مقابله با رویدادهای دکمه‌ها
- تمیز‌کاری منابع هنگام تغییر درس
- هماهنگ‌سازی پخش/توقف

---

## 🎯 درس‌های با تجسمات

### درس ۲: رفتار باکتری
**Canvas ID:** `bacteriaMotionCanvas`

```html
<div class="lesson-canvas-container" id="bacteriaMotionCanvas"></div>
<div class="canvas-controls">
  <button class="btn btn--sm btn--primary" id="startMotionBtn">
    <i class="fas fa-play"></i> شروع
  </button>
  <button class="btn btn--sm btn--outline" id="stopMotionBtn" disabled>
    <i class="fas fa-pause"></i> توقف
  </button>
  <button class="btn btn--sm btn--outline" id="resetMotionBtn">
    <i class="fas fa-redo"></i> تازه‌سازی
  </button>
</div>
```

### درس ۳: شیمی‌جویی
**Canvas ID:** `chemotaxisCanvas`

```html
<div class="lesson-canvas-container" id="chemotaxisCanvas"></div>
<div class="canvas-controls">
  <button class="btn btn--sm btn--primary" id="startChemotaxisBtn">
    <i class="fas fa-play"></i> شروع انیمیشن
  </button>
  <button class="btn btn--sm btn--outline" id="stopChemotaxisBtn">
    <i class="fas fa-pause"></i> توقف
  </button>
</div>
```

### درس ۴: مراحل الگوریتم
**Canvas ID:** `algorithmStepsCanvas`

```html
<div class="lesson-canvas-container" id="algorithmStepsCanvas"></div>
<div class="canvas-controls">
  <button class="btn btn--sm btn--outline" id="prevStepBtn">
    <i class="fas fa-arrow-right"></i> قبلی
  </button>
  <button class="btn btn--sm btn--primary" id="startStepsBtn">
    <i class="fas fa-play"></i> شروع اتوماتیک
  </button>
  <button class="btn btn--sm btn--outline" id="nextStepBtn">
    بعدی <i class="fas fa-arrow-left"></i>
  </button>
</div>
```

---

## 🎨 قالب‌بندی و استایل‌ها

### CSS کلاس‌های موجود:
```css
.lesson-canvas-container  /* کانتینر بوم نقاشی */
.canvas-controls          /* کنترل دکمه‌ها */
.canvas-control-btn       /* دکمه‌های دم دارانجام */
.btn--sm                  /* دکمه‌های کوچک */
.btn--primary             /* دکمه اصلی */
.btn--outline             /* دکمه رئوس‌تیک */
```

### رنگ‌ها و تم:
- **روشن:** پس‌زمینه #f9fafb، متن #1f2937
- **تاریک:** پس‌زمینه #1a1a1a، متن #e5e7eb
- **شطرنج:** شبکه جداکننده برای ملاحظه بهتر

---

## 🔧 نحوه کارکرد ادغام

### مسیر اجرا:
1. **کلیک درس** → `education.html` میں درس بٹن دبایا جاتا ہے
2. **درس لوڈ ہوتا ہے** → `app-config.js` میں `loadLesson()` کال ہوتا ہے
3. **تجسم ابھی کریں** → `educationManager.initializeVisualizers(lessonId)` 
4. **Canvas تیار ہو** → HTML میں `<div>` سے Canvas بنایا جاتا ہے
5. **رویداد‌ها متصل** → دکمے‌ها کی رویداد‌ها سنی جاتے ہیں

---

## 🐛 رفع مسائل عام

### Canvas خالی نہیں دیکھ رہا?
```javascript
// چیک کریں کہ canvas element موجود ہے
console.log(document.getElementById('chemotaxisCanvas'));

// Canvas کو resized کریں
visualizer.resizeCanvas();
```

### دکمے کام نہیں کر رہے؟
```javascript
// صحیح ID استعمال کریں
console.log(document.getElementById('startMotionBtn'));

// Event listeners کو دوبارہ متصل کریں
educationManager.initializeVisualizers('bacteria');
```

### Theme تبدیل نہیں ہو رہی?
```javascript
// Dark mode class چیک کریں
console.log(document.body.classList.contains('dark-mode'));
```

---

## 📈 بہتریاں اور توسیعات

**مستقبل میں شامل کریں:**

1. **تعاملی پیرامیٹرز:**
   - رفتار کو رفتار سے تبدیل کریں
   - باکتری کی تعداد منتخب کریں
   - غذائی منبع کی جگہ تبدیل کریں

2. **شماریات:
   - متوسط فٹنس کا گراف
   - جزیرہ اشتعال کا ڈیٹا
   - convergence curve

3. **موازنہ:** 
   - GA اور PSO کے ساتھ موازنہ کے رویے
   - کارکردگی میٹرکس

4. **متحرک متن:**
   - اردو/فارسی متن کے ساتھ مرحلہ وار وضاحت
   - رئیل ٹائم اعدادوشمار

---

## 📚 فائل‌های حوالہ

- `src/ui/managers/bacterial-visualizer.js` - باکٹیریا حرکت
- `src/ui/managers/behavior-visualizer.js` - رفتار کے مراحل
- `src/ui/managers/education.manager.js` - تجسم کی ترتیب
- `src/config/app-config.js` - ادغام
- `education.html` - درس کا صفحہ

---

**تیار شدہ:** December 15, 2025  
**ورژن:** 1.0.0 (Visualization Update)
